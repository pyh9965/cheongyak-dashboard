# 모집공고 상세정보 구현 완전 가이드

> **다른 AI가 바로 적용 가능한 실전 가이드**  
> Next.js 14 + TypeScript 환경에서 청약홈 모집공고 상세 정보를 구현하는 방법을 단계별로 정리한 문서입니다.

---

## 📋 목차

1. [개요](#개요)
2. [API 설정](#api-설정)
3. [데이터 타입 정의](#데이터-타입-정의)
4. [데이터 통합 로직](#데이터-통합-로직)
5. [포맷팅 함수](#포맷팅-함수)
6. [청약 접수 결과 테이블](#청약-접수-결과-테이블)
7. [상세 정보 섹션](#상세-정보-섹션)
8. [에러 처리](#에러-처리)
9. [완전한 예제 코드](#완전한-예제-코드)
10. [트러블슈팅](#트러블슈팅)

---

## 개요

### 기능 소개

모집공고 상세 정보 페이지는 다음 기능을 제공합니다:

1. **모집공고 기본 정보**
   - 주택명, 공급위치, 총 공급 세대수
   - 사업주체, 관리기관 정보
   - 청약 일정 (접수 시작/종료, 당첨자 발표일)

2. **청약 접수 결과**
   - 타입별 공급 세대 (일반/특별/합계)
   - 특별공급 접수 현황 (카테고리별 상세)
   - 1순위/2순위 접수 현황 (해당/기타 지역 구분)
   - 경쟁률 계산 및 잔여/초과 표시
   - 합계 행 제공

3. **동적 데이터 로딩**
   - 행 선택 시 해당 모집공고의 상세 정보 조회
   - 여러 데이터셋 병렬 호출
   - 부분 성공 시에도 데이터 표시

### 아키텍처

```
[행 선택] (모집공고 목록)
    ↓
[상세 정보 조회] (fetchDetail)
    ↓
[API 라우트] (/api/cheongyak?dataset=notice,noticeModel,noticeCompetition,noticeSpecial)
    ↓
[4개 데이터셋 병렬 호출]
    ├─ notice (기본 정보)
    ├─ noticeModel (모델 정보)
    ├─ noticeCompetition (경쟁률)
    └─ noticeSpecial (특별공급)
    ↓
[데이터 통합] (buildApplicationRows)
    ↓
[UI 렌더링]
```

---

## API 설정

### 1. API 라우트 설정

`src/app/api/cheongyak/route.ts`에 다음 데이터셋들이 설정되어 있어야 합니다:

```typescript
const DATASETS = {
  // 모집공고 기본 정보
  notice: {
    endpoint: "getAPTLttotPblancDetail",
    service: "ApplyhomeInfoDetailSvc",
    includePaging: false,
    requiredParams: ["houseManageNo", "pblancNo"],
    useHousePblancCond: true,
  },
  
  // 모델 정보 (타입별 공급 세대)
  noticeModel: {
    endpoint: "getAPTLttotPblancMdl",
    service: "ApplyhomeInfoDetailSvc",
    includePaging: false,
    requiredParams: ["houseManageNo", "pblancNo"],
    useHousePblancCond: true,
  },
  
  // 경쟁률 정보 (1순위/2순위)
  noticeCompetition: {
    endpoint: "getAPTLttotPblancCmpet",
    service: "ApplyhomeInfoCmpetRtSvc",
    includePaging: false,
    pagingMode: "page",
    requiredParams: ["houseManageNo", "pblancNo"],
    useHousePblancCond: true,
    defaultPerPage: 100,
  },
  
  // 특별공급 접수 현황
  noticeSpecial: {
    endpoint: "getAPTSpsplyReqstStus",
    service: "ApplyhomeInfoCmpetRtSvc",
    includePaging: false,
    pagingMode: "page",
    requiredParams: ["houseManageNo", "pblancNo"],
    useHousePblancCond: true,
    defaultPerPage: 100,
  },
} as const satisfies Record<string, DatasetConfig>;
```

### 2. API 호출 예시

```typescript
// 상세 정보 조회
GET /api/cheongyak?dataset=notice,noticeModel,noticeCompetition,noticeSpecial&houseManageNo=A11078302&pblancNo=2023000001

// 응답 형식
{
  "datasets": {
    "notice": [...],           // 기본 정보
    "noticeModel": [...],      // 모델 정보
    "noticeCompetition": [...], // 경쟁률
    "noticeSpecial": [...]     // 특별공급
  },
  "metadata": {},
  "errors": {
    "noticeModel": "..." // 일부 실패 가능
  }
}
```

---

## 데이터 타입 정의

### 1. API 응답 타입

```typescript
type ApiValue = string | number | null | undefined;

// 모델 정보 응답
type NoticeModelApiRow = {
  MODEL_NO?: ApiValue;                    // 모델 번호
  HOUSE_TY?: ApiValue;                     // 주택형
  SUPLY_AR?: ApiValue;                     // 공급 면적 (㎡)
  SUPLY_HSHLDCO?: ApiValue;                // 일반 공급 세대수
  SPSPLY_HSHLDCO?: ApiValue;               // 특별 공급 세대수
  LTTOT_TOP_AMOUNT?: ApiValue;             // 분양 최고가 (천원)
  INSTT_RECOMEND_HSHLDCO?: ApiValue;       // 기관추천 세대수
  LFE_FRST_HSHLDCO?: ApiValue;             // 생애최초 세대수
  MNYCH_HSHLDCO?: ApiValue;                // 신혼부부 세대수
  NWWDS_HSHLDCO?: ApiValue;                // 다자녀 세대수
  OLD_PARNTS_SUPORT_HSHLDCO?: ApiValue;    // 노부모부양 세대수
  ETC_HSHLDCO?: ApiValue;                  // 기타 세대수
  TRANSR_INSTT_ENFSN_HSHLDCO?: ApiValue;   // 이전기관 세대수
  YGMN_HSHLDCO?: ApiValue;                 // 영구임대 세대수
  [key: string]: ApiValue;
};

// 경쟁률 정보 응답
type NoticeCompetitionApiRow = {
  MODEL_NO?: ApiValue;                     // 모델 번호
  HOUSE_TY?: ApiValue;                     // 주택형
  SUBSCRPT_RANK_CODE?: ApiValue;           // 순위 코드 (1: 1순위, 2: 2순위)
  RESIDE_SECD?: ApiValue;                  // 거주 지역 코드 (01: 해당, 02: 기타)
  RESIDE_SENM?: ApiValue;                  // 거주 지역명
  SUPLY_HSHLDCO?: ApiValue;                // 공급 세대수
  REQ_CNT?: ApiValue;                      // 접수 건수
};

// 특별공급 접수 현황 응답
type NoticeSpecialApiRow = {
  MODEL_NO?: ApiValue;
  HOUSE_TY?: ApiValue;
  INSTT_RECOMEND_DCSN_CNT?: ApiValue;      // 기관추천 결정 건수
  INSTT_RECOMEND_HSHLDCO?: ApiValue;       // 기관추천 세대수
  INSTT_RECOMEND_PREPAR_CNT?: ApiValue;    // 기관추천 준비 건수
  CRSPAREA_LFE_FRST_CNT?: ApiValue;        // 해당지역 생애최초 건수
  CRSPAREA_MNYCH_CNT?: ApiValue;           // 해당지역 신혼부부 건수
  CRSPAREA_NWBB_NWBBSHR_CNT?: ApiValue;    // 해당지역 기타 건수
  CRSPAREA_NWWDS_NMTW_CNT?: ApiValue;      // 해당지역 다자녀 건수
  CRSPAREA_OPS_CNT?: ApiValue;             // 해당지역 노부모부양 건수
  CRSPAREA_YGMN_CNT?: ApiValue;            // 해당지역 영구임대 건수
  CTPRVN_LFE_FRST_CNT?: ApiValue;          // 시도 생애최초 건수
  CTPRVN_MNYCH_CNT?: ApiValue;             // 시도 신혼부부 건수
  CTPRVN_NWBB_NWBBSHR_CNT?: ApiValue;      // 시도 기타 건수
  CTPRVN_NWWDS_NMTW_CNT?: ApiValue;        // 시도 다자녀 건수
  CTPRVN_OPS_CNT?: ApiValue;               // 시도 노부모부양 건수
  CTPRVN_YGMN_CNT?: ApiValue;              // 시도 영구임대 건수
  ETC_AREA_LFE_FRST_CNT?: ApiValue;        // 기타 지역 생애최초 건수
  ETC_AREA_MNYCH_CNT?: ApiValue;           // 기타 지역 신혼부부 건수
  ETC_AREA_NWBB_NWBBSHR_CNT?: ApiValue;    // 기타 지역 기타 건수
  ETC_AREA_NWWDS_NMTW_CNT?: ApiValue;      // 기타 지역 다자녀 건수
  ETC_AREA_OPS_CNT?: ApiValue;             // 기타 지역 노부모부양 건수
  ETC_AREA_YGMN_CNT?: ApiValue;            // 기타 지역 영구임대 건수
  TRANSR_INSTT_ENFSN_CNT?: ApiValue;       // 이전기관 건수
  TRANSR_INSTT_ENFSN_HSHLDCO?: ApiValue;   // 이전기관 세대수
  YGMN_HSHLDCO?: ApiValue;                 // 영구임대 세대수
  [key: string]: ApiValue;
};

// 상세 정보 응답
type NoticeDetailRow = Record<string, unknown>;
```

### 2. 변환된 데이터 타입

```typescript
// 경쟁률 단계 정보
type ApplicationStage = {
  target: number | null;           // 대상 세대수
  request: number | null;          // 접수 건수
  rate: number | null;             // 경쟁률
  remaining: number | null;        // 잔여/초과 세대수
  localRequest?: number | null;    // 해당 지역 접수 (1순위/2순위)
  etcRequest?: number | null;      // 기타 지역 접수 (1순위/2순위)
  breakdown?: Record<string, number>; // 특별공급 상세 내역
};

// 청약 접수 행 데이터
type ApplicationRow = {
  modelNo: string;                 // 모델 번호 (고유 키)
  houseType: string;               // 주택형 표시명
  areaSqm: number | null;          // 전용면적 (㎡)
  areaPyeong: number | null;       // 전용평형 (평)
  priceThousand: number | null;    // 최고 분양가 (천원)
  supplyGeneral: number;           // 일반 공급 세대수
  supplySpecial: number;           // 특별 공급 세대수
  supplyTotal: number;             // 총 공급 세대수
  specialRequests: Record<string, number | null>; // 특별공급 카테고리별 접수
  stages: {
    special: ApplicationStage;     // 특별공급 단계
    rank1: ApplicationStage;       // 1순위 단계
    rank2: ApplicationStage;       // 2순위 단계
    total: ApplicationStage;        // 합계 단계
  };
};
```

---

## 데이터 통합 로직

### 1. 핵심 함수: buildApplicationRows

이 함수는 3개의 API 데이터셋을 받아서 하나의 통합된 테이블 데이터로 변환합니다.

```typescript
function buildApplicationRows(
  models: NoticeModelApiRow[],
  competition: NoticeCompetitionApiRow[],
  special: NoticeSpecialApiRow[],
): { 
  rows: ApplicationRow[]; 
  missingSpecialRequests: boolean; 
  totals: ApplicationRow | null 
} {
  // 1. 경쟁률 데이터 집계
  const compMap = new Map<string, CompAccumulator>();
  
  // 2. 특별공급 데이터 집계
  const specialMap = new Map<string, SpecialAccumulator>();
  
  // 3. 모델 데이터를 기반으로 행 생성
  const rows: ApplicationRow[] = models.map(...).filter(...).sort(...);
  
  // 4. 합계 행 계산
  const totals = rows.reduce(...);
  
  return { rows, missingSpecialRequests, totals };
}
```

### 2. 경쟁률 데이터 집계

```typescript
type CompAccumulator = {
  rank1Target: number | null;
  rank1Requests: number;
  rank1Local: number;      // 해당 지역 접수
  rank1Etc: number;         // 기타 지역 접수
  rank2Target: number | null;
  rank2Requests: number;
  rank2Local: number;
  rank2Etc: number;
};

const compMap = new Map<string, CompAccumulator>();

competition.forEach((item) => {
  const modelNo = String(item.MODEL_NO ?? item.HOUSE_TY ?? "").trim();
  if (!modelNo) return;
  
  const stageCode = toInt(item.SUBSCRPT_RANK_CODE); // 1: 1순위, 2: 2순위
  const region = String(item.RESIDE_SECD ?? "");    // 01: 해당, 02: 기타
  const target = toInt(item.SUPLY_HSHLDCO);
  const request = toInt(item.REQ_CNT);

  const acc = compMap.get(modelNo) ?? {
    rank1Target: null,
    rank1Requests: 0,
    rank1Local: 0,
    rank1Etc: 0,
    rank2Target: null,
    rank2Requests: 0,
    rank2Local: 0,
    rank2Etc: 0,
  };

  if (stageCode === 1) {
    // 1순위 처리
    if (acc.rank1Target === null && target > 0) acc.rank1Target = target;
    acc.rank1Requests += request;
    if (region === "01") acc.rank1Local += request;
    else if (region === "02") acc.rank1Etc += request;
  } else if (stageCode === 2) {
    // 2순위 처리
    if (acc.rank2Target === null && target > 0) acc.rank2Target = target;
    acc.rank2Requests += request;
    if (region === "01") acc.rank2Local += request;
    else if (region === "02") acc.rank2Etc += request;
  }

  compMap.set(modelNo, acc);
});
```

### 3. 특별공급 데이터 집계

```typescript
type SpecialAccumulator = {
  total: number;
  instt: number;          // 기관추천
  newlywed: number;       // 신혼부부
  life: number;          // 생애최초
  multi: number;         // 다자녀가구
  oldParent: number;     // 노부모부양
  etc: number;          // 기타
  transfer: number;     // 이전기관
  young: number;        // 영구임대
};

const specialMap = new Map<string, SpecialAccumulator>();

special.forEach((item) => {
  const keys = [
    String(item.MODEL_NO ?? "").trim(), 
    String(item.HOUSE_TY ?? "").trim()
  ].filter((key) => key.length > 0);
  if (keys.length === 0) return;

  // 각 카테고리별 접수 건수 합산 (해당지역 + 시도 + 기타지역)
  const instt = toInt(item.INSTT_RECOMEND_DCSN_CNT);
  const newlywed = 
    toInt(item.CRSPAREA_MNYCH_CNT) + 
    toInt(item.ETC_AREA_MNYCH_CNT) + 
    toInt(item.CTPRVN_MNYCH_CNT);
  const life = 
    toInt(item.CRSPAREA_LFE_FRST_CNT) + 
    toInt(item.ETC_AREA_LFE_FRST_CNT) + 
    toInt(item.CTPRVN_LFE_FRST_CNT);
  const multi = 
    toInt(item.CRSPAREA_NWWDS_NMTW_CNT) + 
    toInt(item.ETC_AREA_NWWDS_NMTW_CNT) + 
    toInt(item.CTPRVN_NWWDS_NMTW_CNT);
  const oldParent = 
    toInt(item.CRSPAREA_OPS_CNT) + 
    toInt(item.ETC_AREA_OPS_CNT) + 
    toInt(item.CTPRVN_OPS_CNT);
  const etc = 
    toInt(item.CRSPAREA_NWBB_NWBBSHR_CNT) + 
    toInt(item.ETC_AREA_NWBB_NWBBSHR_CNT) + 
    toInt(item.CTPRVN_NWBB_NWBBSHR_CNT);
  const transfer = toInt(item.TRANSR_INSTT_ENFSN_CNT);
  const young = 
    toInt(item.CRSPAREA_YGMN_CNT) + 
    toInt(item.ETC_AREA_YGMN_CNT) + 
    toInt(item.CTPRVN_YGMN_CNT);
  const total = instt + newlywed + life + multi + oldParent + etc + transfer + young;

  keys.forEach((key) => {
    if (!key) return;
    const acc = specialMap.get(key) ?? {
      total: 0, instt: 0, newlywed: 0, life: 0,
      multi: 0, oldParent: 0, etc: 0, transfer: 0, young: 0,
    };

    acc.instt += instt;
    acc.newlywed += newlywed;
    acc.life += life;
    acc.multi += multi;
    acc.oldParent += oldParent;
    acc.etc += etc;
    acc.transfer += transfer;
    acc.young += young;
    acc.total += total;

    specialMap.set(key, acc);
  });
});
```

### 4. 행 데이터 생성

```typescript
const rows: ApplicationRow[] = models
  .map((model) => {
    const modelNo = String(model.MODEL_NO ?? model.HOUSE_TY ?? "").trim();
    if (!modelNo) return null;

    const houseType = String(model.HOUSE_TY ?? model.MODEL_NO ?? "-").trim();
    
    // 면적 계산
    const areaSqmRaw = toFloat(model.SUPLY_AR);
    const areaSqm = areaSqmRaw > 0 ? areaSqmRaw : null;
    const areaPyeong = areaSqm ? areaSqm / 3.3058 : null;
    
    // 공급 세대수
    const specialSupply = toInt(model.SPSPLY_HSHLDCO);
    const generalSupply = toInt(model.SUPLY_HSHLDCO);
    const supplyTotal = specialSupply + generalSupply;
    
    // 분양가
    const priceThousand = toInt(model.LTTOT_TOP_AMOUNT);
    
    // 경쟁률 데이터 매핑
    const comp = compMap.get(modelNo) ?? {
      rank1Target: null, rank1Requests: 0, rank1Local: 0, rank1Etc: 0,
      rank2Target: null, rank2Requests: 0, rank2Local: 0, rank2Etc: 0,
    };
    
    // 대상 세대수 추정 (경쟁률 데이터가 없으면 모델 데이터 사용)
    const rank1Target = comp.rank1Target 
      ?? (generalSupply > 0 ? generalSupply : null) 
      ?? (supplyTotal > 0 ? supplyTotal : null);
    const rank2Target = comp.rank2Target ?? rank1Target;
    const totalTarget = rank1Target ?? rank2Target ?? (generalSupply > 0 ? generalSupply : null);
    const totalRequest = comp.rank1Requests + comp.rank2Requests;
    
    // 특별공급 세대수 내역
    const specialBreakdown: Record<string, number> = {
      기관추천: toInt(model.INSTT_RECOMEND_HSHLDCO),
      "신혼부부": toInt(model.MNYCH_HSHLDCO),
      "생애최초": toInt(model.LFE_FRST_HSHLDCO),
      "다자녀": toInt(model.NWWDS_HSHLDCO),
      "노부모부양": toInt(model.OLD_PARNTS_SUPORT_HSHLDCO),
      "기타": toInt(model.ETC_HSHLDCO),
    };
    
    // 특별공급 접수 내역
    const specialAgg = specialMap.get(modelNo) ?? specialMap.get(houseType);
    const specialRequests: Record<string, number | null> = {
      기관추천: specialAgg ? specialAgg.instt : null,
      신혼부부: specialAgg ? specialAgg.newlywed : null,
      생애최초: specialAgg ? specialAgg.life : null,
      다자녀가구: specialAgg ? specialAgg.multi : null,
      노부모부양: specialAgg ? specialAgg.oldParent : null,
      기타: specialAgg ? specialAgg.etc : null,
      이전기관: specialAgg ? specialAgg.transfer : null,
      영구임대: specialAgg ? specialAgg.young : null,
    };
    const specialRequestTotal = specialAgg ? specialAgg.total : null;
    
    return {
      modelNo,
      houseType,
      areaSqm,
      areaPyeong,
      priceThousand: priceThousand > 0 ? priceThousand : null,
      supplyGeneral: generalSupply,
      supplySpecial: specialSupply,
      supplyTotal,
      specialRequests,
      stages: {
        special: {
          target: specialSupply > 0 ? specialSupply : null,
          request: specialRequestTotal,
          rate: specialSupply && specialRequestTotal !== null 
            ? specialRequestTotal / specialSupply 
            : specialSupply ? 0 : null,
          remaining: specialSupply !== null && specialRequestTotal !== null 
            ? specialSupply - specialRequestTotal 
            : null,
          breakdown: specialBreakdown,
        },
        rank1: {
          target: rank1Target,
          request: comp.rank1Requests ?? null,
          rate: rank1Target && comp.rank1Requests 
            ? comp.rank1Requests / rank1Target 
            : rank1Target ? 0 : null,
          remaining: rank1Target !== null 
            ? rank1Target - comp.rank1Requests 
            : null,
          localRequest: comp.rank1Local,
          etcRequest: comp.rank1Etc,
        },
        rank2: {
          target: rank2Target,
          request: comp.rank2Requests ?? null,
          rate: rank2Target && comp.rank2Requests 
            ? comp.rank2Requests / rank2Target 
            : rank2Target ? 0 : null,
          remaining: rank2Target !== null 
            ? rank2Target - comp.rank2Requests 
            : null,
          localRequest: comp.rank2Local,
          etcRequest: comp.rank2Etc,
        },
        total: {
          target: totalTarget,
          request: totalTarget !== null ? totalRequest : null,
          rate: totalTarget && totalRequest 
            ? totalRequest / totalTarget 
            : totalTarget ? 0 : null,
          remaining: totalTarget !== null 
            ? totalTarget - totalRequest 
            : null,
        },
      },
    } satisfies ApplicationRow;
  })
  .filter((value): value is ApplicationRow => value !== null)
  .sort((a, b) => {
    // 주택형 정렬 (숫자 우선, 접미사 고려)
    const aKey = parseHouseTypeKey(a.houseType);
    const bKey = parseHouseTypeKey(b.houseType);
    
    if (aKey.base !== null && bKey.base !== null && aKey.base !== bKey.base) {
      return aKey.base - bKey.base;
    }
    
    if (aKey.suffix && bKey.suffix && aKey.suffix !== bKey.suffix) {
      return aKey.suffix.localeCompare(bKey.suffix, "en", { sensitivity: "base" });
    }
    
    if (aKey.numeric !== null && bKey.numeric !== null && aKey.numeric !== bKey.numeric) {
      return aKey.numeric - bKey.numeric;
    }
    
    return aKey.raw.localeCompare(bKey.raw, "en", { numeric: true, sensitivity: "base" });
  });
```

### 5. 합계 행 계산

```typescript
const totals = rows.reduce<ApplicationRow | null>((acc, row, index) => {
  if (index === 0) {
    // 첫 번째 행으로 초기화
    return {
      modelNo: "TOTAL",
      houseType: "합계",
      areaSqm: null,
      areaPyeong: null,
      priceThousand: null,
      supplyGeneral: row.supplyGeneral,
      supplySpecial: row.supplySpecial,
      supplyTotal: row.supplyTotal,
      specialRequests: { ...row.specialRequests },
      stages: {
        special: {
          target: row.stages.special.target ?? 0,
          request: row.stages.special.request ?? 0,
          rate: null,
          remaining: null,
          breakdown: row.stages.special.breakdown ? { ...row.stages.special.breakdown } : undefined,
        },
        rank1: {
          target: row.stages.rank1.target ?? 0,
          request: row.stages.rank1.request ?? 0,
          rate: null,
          remaining: null,
          localRequest: row.stages.rank1.localRequest ?? 0,
          etcRequest: row.stages.rank1.etcRequest ?? 0,
        },
        rank2: {
          target: row.stages.rank2.target ?? 0,
          request: row.stages.rank2.request ?? 0,
          rate: null,
          remaining: null,
          localRequest: row.stages.rank2.localRequest ?? 0,
          etcRequest: row.stages.rank2.etcRequest ?? 0,
        },
        total: {
          target: row.stages.total.target ?? 0,
          request: row.stages.total.request ?? 0,
          rate: null,
          remaining: null,
        },
      },
    } satisfies ApplicationRow;
  }

  if (!acc) return acc;

  // 각 항목 누적
  acc.supplyGeneral += row.supplyGeneral;
  acc.supplySpecial += row.supplySpecial;
  acc.supplyTotal += row.supplyTotal;
  
  // 특별공급 접수 누적
  acc.specialRequests = acc.specialRequests ?? {};
  Object.entries(row.specialRequests).forEach(([key, value]) => {
    acc!.specialRequests[key] = (acc!.specialRequests[key] ?? 0) + (value ?? 0);
  });
  
  // 각 단계별 누적
  acc.stages.special.target = (acc.stages.special.target ?? 0) + (row.stages.special.target ?? 0);
  acc.stages.special.request = (acc.stages.special.request ?? 0) + (row.stages.special.request ?? 0);
  
  acc.stages.rank1.target = (acc.stages.rank1.target ?? 0) + (row.stages.rank1.target ?? 0);
  acc.stages.rank1.request = (acc.stages.rank1.request ?? 0) + (row.stages.rank1.request ?? 0);
  acc.stages.rank1.localRequest = (acc.stages.rank1.localRequest ?? 0) + (row.stages.rank1.localRequest ?? 0);
  acc.stages.rank1.etcRequest = (acc.stages.rank1.etcRequest ?? 0) + (row.stages.rank1.etcRequest ?? 0);
  
  acc.stages.rank2.target = (acc.stages.rank2.target ?? 0) + (row.stages.rank2.target ?? 0);
  acc.stages.rank2.request = (acc.stages.rank2.request ?? 0) + (row.stages.rank2.request ?? 0);
  acc.stages.rank2.localRequest = (acc.stages.rank2.localRequest ?? 0) + (row.stages.rank2.localRequest ?? 0);
  acc.stages.rank2.etcRequest = (acc.stages.rank2.etcRequest ?? 0) + (row.stages.rank2.etcRequest ?? 0);
  
  acc.stages.total.target = (acc.stages.total.target ?? 0) + (row.stages.total.target ?? 0);
  acc.stages.total.request = (acc.stages.total.request ?? 0) + (row.stages.total.request ?? 0);
  
  return acc;
}, null);

// 합계 행의 경쟁률 계산
if (totals) {
  const fillRate = (stage: ApplicationStage) => {
    if (stage.target && stage.request !== null) {
      stage.rate = stage.target > 0 ? stage.request / stage.target : null;
      stage.remaining = stage.target - stage.request;
    } else {
      stage.rate = null;
      stage.remaining = null;
    }
  };

  fillRate(totals.stages.rank1);
  fillRate(totals.stages.rank2);
  fillRate(totals.stages.total);
}
```

---

## 포맷팅 함수

### 1. 숫자 변환 함수

```typescript
type ApiValue = string | number | null | undefined;

function toInt(value: ApiValue): number {
  if (value === null || value === undefined) return 0;
  const raw = typeof value === "number" 
    ? value 
    : Number(String(value).replace(/[^0-9-]/g, ""));
  return Number.isFinite(raw) ? raw : 0;
}

function toFloat(value: ApiValue): number {
  if (value === null || value === undefined) return 0;
  const raw = typeof value === "number" 
    ? value 
    : Number(String(value).replace(/[^0-9.-]/g, ""));
  return Number.isFinite(raw) ? raw : 0;
}
```

### 2. 숫자 포맷팅 함수

```typescript
function formatNumber(value: number | null | undefined, fallback = "-"): string {
  if (value === null || value === undefined) return fallback;
  return value.toLocaleString();
}

function formatPriceTenThousand(value: number | null): string {
  if (value === null || value === undefined) return "-";
  if (!Number.isFinite(value)) return "-";
  return `${value.toLocaleString()} 만원`;
}

function formatRate(request: number | null, target: number | null): string {
  if (request === null || target === null || target <= 0) return "-";
  const ratio = request / target;
  if (!Number.isFinite(ratio)) return "-";
  const precision = ratio >= 10 ? 1 : 2; // 10 이상이면 소수점 1자리, 미만이면 2자리
  return `${ratio.toFixed(precision)} : 1`;
}

function formatDifference(remaining: number | null): string | null {
  if (remaining === null) return null;
  if (remaining === 0) return "잔여 0";
  const label = remaining > 0 ? "잔여" : "초과";
  return `${label} ${Math.abs(remaining).toLocaleString()}`;
}

function formatSpecialRequestEntries(requests: Record<string, number | null>): string[] {
  return Object.entries(requests)
    .filter(([, value]) => value !== null && value !== undefined && value !== 0)
    .map(([label, value]) => `${label} ${(value ?? 0).toLocaleString()}`);
}
```

### 3. 주택형 파싱 함수

```typescript
type HouseTypeKey = {
  numeric: number | null;
  base: number | null;
  suffix: string;
  raw: string;
};

function parseHouseTypeKey(value: string): HouseTypeKey {
  const trimmed = value.trim();
  const match = trimmed.match(/^(\d+(?:\.\d+)?)(.*)$/);
  if (!match) {
    return { numeric: null, base: null, suffix: trimmed.toUpperCase(), raw: trimmed };
  }
  const numeric = Number.parseFloat(match[1]);
  const suffix = match[2]?.trim().toUpperCase() ?? "";
  return {
    numeric: Number.isFinite(numeric) ? numeric : null,
    base: Number.isFinite(numeric) ? Math.trunc(numeric) : null,
    suffix,
    raw: trimmed,
  };
}
```

---

## 청약 접수 결과 테이블

### 1. 테이블 구조

```typescript
{applicationRows.length > 0 && (
  <section style={{ marginBottom: 24, border: "1px solid #e5e7eb", borderRadius: 12, background: "#fff" }}>
    <header style={{ padding: "16px 20px", borderBottom: "1px solid #e5e7eb", background: "#f9fafb" }}>
      <h3 style={{ fontSize: 18, fontWeight: 600 }}>청약 접수 결과</h3>
      <p style={{ fontSize: 12, color: "#6b7280", marginTop: 4 }}>
        API 제공 데이터 기준(대상·접수·경쟁률, 특별공급 실접수 포함)으로 구성했습니다.
      </p>
    </header>
    
    <div style={{ overflowX: "auto" }}>
      <table style={{ width: "100%", borderCollapse: "collapse", minWidth: 1100 }}>
        {/* 테이블 헤더 및 본문 */}
      </table>
    </div>
    
    <div style={{ padding: "12px 20px", fontSize: 12, color: "#6b7280", borderTop: "1px solid #e5e7eb" }}>
      <div>※ 경쟁률 = 접수 ÷ 대상, 금액 단위: 천원 기준</div>
      {missingSpecialRequestData && (
        <div>※ 특별공급 접수 인원은 현재 공개된 API에서 제공되지 않아 '-'로 표기됩니다.</div>
      )}
    </div>
  </section>
)}
```

### 2. 테이블 헤더

```typescript
<thead>
  <tr style={{ background: "#f3f4f6" }}>
    <th rowSpan={2}>타입</th>
    <th rowSpan={2}>전용면적(㎡)</th>
    <th rowSpan={2}>전용평형(평)</th>
    <th colSpan={3}>공급세대</th>
    <th rowSpan={2}>최고 분양가(만원)</th>
    <th colSpan={3}>특별공급</th>
    <th colSpan={3}>1순위 접수</th>
    <th colSpan={3}>2순위 접수</th>
    <th colSpan={3}>합계</th>
  </tr>
  <tr style={{ background: "#f3f4f6" }}>
    <th>일반</th>
    <th>특별</th>
    <th>합계</th>
    <th>대상</th>
    <th>접수</th>
    <th>경쟁률</th>
    <th>대상</th>
    <th>접수</th>
    <th>경쟁률</th>
    <th>대상</th>
    <th>접수</th>
    <th>경쟁률</th>
    <th>대상</th>
    <th>접수</th>
    <th>경쟁률</th>
  </tr>
</thead>
```

### 3. 테이블 본문 (행 렌더링)

```typescript
<tbody>
  {applicationRows.map((row) => (
    <tr key={row.modelNo}>
      {/* 타입 */}
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, fontWeight: 600 }}>
        {row.houseType}
      </td>
      
      {/* 전용면적 */}
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {row.areaSqm ? `${row.areaSqm.toFixed(2)}` : "-"}
      </td>
      
      {/* 전용평형 */}
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {row.areaPyeong ? `${row.areaPyeong.toFixed(1)}` : "-"}
      </td>
      
      {/* 공급세대 */}
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {formatNumber(row.supplyGeneral)}
      </td>
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {formatNumber(row.supplySpecial)}
      </td>
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {formatNumber(row.supplyTotal)}
      </td>
      
      {/* 최고 분양가 */}
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {formatPriceTenThousand(row.priceThousand)}
      </td>
      
      {/* 특별공급 */}
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {formatNumber(row.stages.special.target)}
      </td>
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13 }}>
        <div style={{ textAlign: "right" }}>{formatNumber(row.stages.special.request)}</div>
        {row.specialRequests && formatSpecialRequestEntries(row.specialRequests).length > 0 && (
          <div style={{ fontSize: 11, color: "#6b7280", textAlign: "right", lineHeight: 1.4 }}>
            {formatSpecialRequestEntries(row.specialRequests).join(" · ")}
          </div>
        )}
      </td>
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {formatRate(row.stages.special.request, row.stages.special.target)}
      </td>
      
      {/* 1순위 접수 */}
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {formatNumber(row.stages.rank1.target)}
      </td>
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13 }}>
        <div style={{ textAlign: "right" }}>{formatNumber(row.stages.rank1.request)}</div>
        {(row.stages.rank1.localRequest ?? 0) + (row.stages.rank1.etcRequest ?? 0) > 0 && (
          <div style={{ fontSize: 11, color: "#6b7280", textAlign: "right" }}>
            해당 {formatNumber(row.stages.rank1.localRequest ?? 0)} / 기타 {formatNumber(row.stages.rank1.etcRequest ?? 0)}
          </div>
        )}
      </td>
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        <div>{formatRate(row.stages.rank1.request, row.stages.rank1.target)}</div>
        {formatDifference(row.stages.rank1.remaining) && (
          <div style={{ fontSize: 11, color: "#6b7280" }}>
            {formatDifference(row.stages.rank1.remaining)}
          </div>
        )}
      </td>
      
      {/* 2순위 접수 */}
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {formatNumber(row.stages.rank2.target)}
      </td>
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13 }}>
        <div style={{ textAlign: "right" }}>{formatNumber(row.stages.rank2.request)}</div>
        {(row.stages.rank2.localRequest ?? 0) + (row.stages.rank2.etcRequest ?? 0) > 0 && (
          <div style={{ fontSize: 11, color: "#6b7280", textAlign: "right" }}>
            해당 {formatNumber(row.stages.rank2.localRequest ?? 0)} / 기타 {formatNumber(row.stages.rank2.etcRequest ?? 0)}
          </div>
        )}
      </td>
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        <div>{formatRate(row.stages.rank2.request, row.stages.rank2.target)}</div>
        {formatDifference(row.stages.rank2.remaining) && (
          <div style={{ fontSize: 11, color: "#6b7280" }}>
            {formatDifference(row.stages.rank2.remaining)}
          </div>
        )}
      </td>
      
      {/* 합계 */}
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {formatNumber(row.stages.total.target)}
      </td>
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        {formatNumber(row.stages.total.request)}
      </td>
      <td style={{ padding: "10px", borderBottom: "1px solid #f3f4f6", fontSize: 13, textAlign: "right" }}>
        <div>{formatRate(row.stages.total.request, row.stages.total.target)}</div>
        {formatDifference(row.stages.total.remaining) && (
          <div style={{ fontSize: 11, color: "#6b7280" }}>
            {formatDifference(row.stages.total.remaining)}
          </div>
        )}
      </td>
    </tr>
  ))}
</tbody>
```

### 4. 합계 행 (tfoot)

```typescript
{applicationTotals && (
  <tfoot>
    <tr style={{ background: "#f9fafb", fontWeight: 600 }}>
      <td style={{ padding: "10px", borderTop: "2px solid #e5e7eb" }}>
        {applicationTotals.houseType}
      </td>
      <td style={{ padding: "10px", borderTop: "2px solid #e5e7eb", textAlign: "right" }}>-</td>
      <td style={{ padding: "10px", borderTop: "2px solid #e5e7eb", textAlign: "right" }}>-</td>
      {/* 각 열에 합계 값 표시 */}
      {/* ... 위와 동일한 구조, applicationTotals 사용 ... */}
    </tr>
  </tfoot>
)}
```

---

## 상세 정보 섹션

### 1. 섹션 설정

```typescript
type FieldConfig = {
  key: string;
  label: string;
};

type SectionConfig = {
  title: string;
  fields: FieldConfig[];
};

const FIELD_SECTIONS: SectionConfig[] = [
  {
    title: "기본 정보",
    fields: [
      { key: "HOUSE_NM", label: "주택명" },
      { key: "HSSPLY_ADRES", label: "공급위치" },
      { key: "TOT_SUPLY_HSHLDCO", label: "총 공급 세대수" },
      { key: "SUPLY_AR", label: "공급 규모" },
      { key: "HMPG_URL", label: "모집공고 URL" },
      { key: "BSNS_MBY_NM", label: "사업주체" },
      { key: "BSNS_MBY_TELNO", label: "사업주체 문의처" },
      { key: "MNGT_INSTT_NM", label: "관리기관" },
      { key: "MNGT_INSTT_TELNO", label: "관리기관 문의처" },
    ],
  },
  {
    title: "청약 일정",
    fields: [
      { key: "RCEPT_BGNDE", label: "청약접수 시작" },
      { key: "RCEPT_ENDDE", label: "청약접수 종료" },
      { key: "PRZWNER_PRESNATN_DE", label: "당첨자 발표일" },
      { key: "CNTRCT_CNCLS_BGNDE", label: "계약 시작" },
      { key: "CNTRCT_CNCLS_ENDDE", label: "계약 종료" },
      { key: "SUBSCRPT_AREA_CODE_NM", label: "접수 지역" },
      { key: "RCEPT_SE_NM", label: "접수 방식" },
      { key: "RCEPT_PLACE", label: "접수 장소" },
    ],
  },
  {
    title: "공급 대상",
    fields: [
      { key: "MODEL_NO", label: "모델 번호" },
      { key: "HOUSE_TY", label: "주택형" },
      { key: "SUPLY_HSHLDCO", label: "일반 공급" },
      { key: "SPECL_SUPLY_HSHLDCO", label: "특별 공급" },
      { key: "LTTOT_TOP_AMOUNT", label: "분양 최고가" },
      { key: "LTTOT_BTM_AMOUNT", label: "분양 최저가" },
      { key: "IMPRMN_BSNS_AT_NM", label: "정비사업 여부" },
    ],
  },
  {
    title: "기타 현황",
    fields: [
      { key: "ETC_MATR_CN", label: "기타 안내" },
      { key: "PBLANC_NO", label: "모집공고번호" },
      { key: "HOUSE_MANAGE_NO", label: "주택관리번호" },
      { key: "GOV_CONT_INFO", label: "정부 지원 안내" },
    ],
  },
];
```

### 2. 섹션 데이터 구조화

```typescript
function buildSectionData(row: NoticeDetailRow) {
  const usedKeys = new Set<string>();
  
  // 정의된 섹션 처리
  const sections = FIELD_SECTIONS.map(({ title, fields }) => {
    const items = fields
      .map(({ key, label }) => {
        const value = row[key];
        if (value === undefined || value === null || value === "") {
          return null;
        }
        usedKeys.add(key);
        return { key, label, value };
      })
      .filter((item): item is { key: string; label: string; value: unknown } => item !== null);
    return { title, items };
  });

  // 추가 필드 처리
  const remaining = Object.entries(row)
    .filter(([key, value]) => !usedKeys.has(key) && value !== null && value !== undefined && value !== "")
    .map(([key, value]) => ({
      key,
      label: FIELD_LABELS[key] ?? key,
      value,
    }));

  if (remaining.length > 0) {
    sections.push({
      title: "추가 필드",
      items: remaining,
    });
  }

  return sections.filter((section) => section.items.length > 0);
}
```

### 3. 중첩 테이블 컴포넌트

```typescript
function NestedTable({ rows }: { rows: Record<string, unknown>[] }) {
  if (rows.length === 0) return null;
  const columns = Array.from(new Set(rows.flatMap((row) => Object.keys(row))));

  return (
    <div style={{ overflowX: "auto" }}>
      <table style={{ width: "100%", borderCollapse: "collapse", minWidth: 480 }}>
        <thead>
          <tr>
            {columns.map((column) => (
              <th key={column} style={{ padding: "6px 8px", border: "1px solid #e5e7eb", background: "#f9fafb", fontSize: 12, fontWeight: 600 }}>
                {FIELD_LABELS[column] ?? column}
              </th>
            ))}
          </tr>
        </thead>
        <tbody>
          {rows.map((row, rowIndex) => (
            <tr key={rowIndex}>
              {columns.map((column) => (
                <td key={column} style={{ padding: "6px 8px", border: "1px solid #e5e7eb", fontSize: 12, color: "#374151" }}>
                  {formatValue(row[column])}
                </td>
              ))}
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

### 4. 상세 정보 렌더링

```typescript
{detailRows.map((row, index) => {
  const sections = buildSectionData(row);
  const houseName = String((row["HOUSE_NM"] ?? "") as string).trim();
  return (
    <article key={index} style={{ marginBottom: 32, border: "1px solid #e5e7eb", borderRadius: 12, overflow: "hidden", background: "#fff" }}>
      <header style={{ padding: "16px 20px", background: "#f3f4f6", borderBottom: "1px solid #e5e7eb" }}>
        <h3 style={{ fontSize: 20, fontWeight: 600 }}>{houseName || "모집공고"}</h3>
        <p style={{ fontSize: 13, color: "#6b7280" }}>
          주택관리번호 {formatValue(row["HOUSE_MANAGE_NO"])}, 모집공고번호 {formatValue(row["PBLANC_NO"])}
        </p>
      </header>

      {sections.map((section) => (
        <section key={section.title} style={{ padding: "16px 20px", borderBottom: "1px solid #f3f4f6" }}>
          <h4 style={{ fontSize: 16, fontWeight: 600, marginBottom: 12 }}>{section.title}</h4>
          <table style={{ width: "100%", borderCollapse: "collapse" }}>
            <tbody>
              {section.items.map((item) => (
                <tr key={item.key}>
                  <th style={{ width: "35%", textAlign: "left", padding: "8px 12px", background: "#f9fafb", border: "1px solid #e5e7eb", fontSize: 13, fontWeight: 600 }}>
                    {item.label}
                  </th>
                  <td style={{ padding: "8px 12px", border: "1px solid #e5e7eb", fontSize: 13, color: "#374151" }}>
                    {Array.isArray(item.value) && item.value.length > 0 && typeof item.value[0] === "object" ? (
                      <NestedTable rows={item.value as Record<string, unknown>[]} />
                    ) : (
                      formatValue(item.value)
                    )}
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </section>
      ))}
    </article>
  );
})}
```

---

## 에러 처리

### 1. 상세 정보 조회 함수

```typescript
const fetchDetail = useCallback(async (houseManageNo: string, pblancNo: string) => {
  if (!houseManageNo || !pblancNo) {
    setDetailRows([]);
    setDetailError("상세 조회에 필요한 식별자가 없습니다.");
    setApplicationRows([]);
    setMissingSpecialRequestData(false);
    return;
  }

  setDetailLoading(true);
  setDetailError(null);
  setDatasetErrors({});
  setApplicationRows([]);
  setMissingSpecialRequestData(false);

  try {
    const params = new URLSearchParams({
      dataset: "notice,noticeModel,noticeCompetition,noticeSpecial",
      houseManageNo,
      pblancNo,
    });
    const res = await fetch(`/api/cheongyak?${params.toString()}`);
    let json: any = {};
    try {
      json = await res.json();
    } catch {
      json = {};
    }

    // 데이터셋별 에러 확인
    if (json.errors) {
      setDatasetErrors(json.errors);
    } else {
      setDatasetErrors({});
    }

    if (!res.ok) {
      const message = typeof json.error === "string" && json.error.trim().length > 0 
        ? json.error 
        : `API 요청 실패 (${res.status})`;
      throw new Error(message);
    }

    // 각 데이터셋 추출
    const rows = Array.isArray(json.datasets?.notice) 
      ? (json.datasets.notice as NoticeDetailRow[]) 
      : [];
    setDetailRows(rows);

    const modelRows = Array.isArray(json.datasets?.noticeModel) 
      ? (json.datasets.noticeModel as NoticeModelApiRow[]) 
      : [];
    const competitionRows = Array.isArray(json.datasets?.noticeCompetition) 
      ? (json.datasets.noticeCompetition as NoticeCompetitionApiRow[]) 
      : [];
    const specialRows = Array.isArray(json.datasets?.noticeSpecial) 
      ? (json.datasets.noticeSpecial as NoticeSpecialApiRow[]) 
      : [];
    
    // 데이터 통합
    const { rows: applicationSummary, missingSpecialRequests, totals } = buildApplicationRows(
      modelRows, 
      competitionRows, 
      specialRows
    );
    setApplicationRows(applicationSummary);
    setMissingSpecialRequestData(missingSpecialRequests);
    setApplicationTotals(totals);
  } catch (err) {
    const message = err instanceof Error ? err.message : "상세 데이터를 불러오지 못했습니다.";
    setDetailError(message);
    setDetailRows([]);
    setApplicationRows([]);
    setMissingSpecialRequestData(false);
    setApplicationTotals(null);
  } finally {
    setDetailLoading(false);
  }
}, []);
```

### 2. 데이터셋별 에러 표시

```typescript
const noticeError = useMemo(() => datasetErrors.notice, [datasetErrors]);
const noticeModelError = useMemo(() => datasetErrors.noticeModel, [datasetErrors]);
const noticeCompetitionError = useMemo(() => datasetErrors.noticeCompetition, [datasetErrors]);
const noticeSpecialError = useMemo(() => datasetErrors.noticeSpecial, [datasetErrors]);

// UI에서 표시
{noticeError && (
  <div style={{ marginBottom: 16, padding: 14, background: "#fef3c7", border: "1px solid #fde68a", borderRadius: 10, color: "#92400e", fontSize: 14 }}>
    모집공고 상세 조회 오류: {noticeError}
  </div>
)}
{noticeModelError && (
  <div style={{ marginBottom: 16, padding: 14, background: "#fef3c7", border: "1px solid #fde68a", borderRadius: 10, color: "#92400e", fontSize: 14 }}>
    주택형별 정보 조회 오류: {noticeModelError}
  </div>
)}
{noticeCompetitionError && (
  <div style={{ marginBottom: 16, padding: 14, background: "#fef3c7", border: "1px solid #fde68a", borderRadius: 10, color: "#92400e", fontSize: 14 }}>
    청약경쟁률 조회 오류: {noticeCompetitionError}
  </div>
)}
{noticeSpecialError && (
  <div style={{ marginBottom: 16, padding: 14, background: "#fef3c7", border: "1px solid #fde68a", borderRadius: 10, color: "#92400e", fontSize: 14 }}>
    특별공급 접수 현황 조회 오류: {noticeSpecialError}
  </div>
)}
```

---

## 완전한 예제 코드

### 1. 상태 관리

```typescript
// 상세 정보 상태
const [detailRows, setDetailRows] = useState<NoticeDetailRow[]>([]);
const [detailLoading, setDetailLoading] = useState(false);
const [detailError, setDetailError] = useState<string | null>(null);
const [datasetErrors, setDatasetErrors] = useState<Record<string, string>>({});

// 청약 접수 결과 상태
const [applicationRows, setApplicationRows] = useState<ApplicationRow[]>([]);
const [missingSpecialRequestData, setMissingSpecialRequestData] = useState(false);
const [applicationTotals, setApplicationTotals] = useState<ApplicationRow | null>(null);

// 선택된 행
const [selected, setSelected] = useState<NoticeListRow | null>(null);
```

### 2. 선택된 행 변경 시 조회

```typescript
useEffect(() => {
  if (!selected?.HOUSE_MANAGE_NO || !selected?.PBLANC_NO) {
    setDetailRows([]);
    setApplicationRows([]);
    setMissingSpecialRequestData(false);
    setApplicationTotals(null);
    return;
  }
  void fetchDetail(String(selected.HOUSE_MANAGE_NO), String(selected.PBLANC_NO));
}, [fetchDetail, selected]);
```

---

## 트러블슈팅

### 1. 데이터가 표시되지 않는 경우

**증상:**
- 청약 접수 결과 테이블이 비어 있음
- 상세 정보가 표시되지 않음

**원인:**
- `buildApplicationRows` 함수에서 데이터 매핑 실패
- API 응답 형식 불일치
- 필수 파라미터 누락

**해결 방법:**

```typescript
// 1. API 응답 확인
console.log("API 응답:", json);
console.log("모델 데이터:", json.datasets?.noticeModel);
console.log("경쟁률 데이터:", json.datasets?.noticeCompetition);
console.log("특별공급 데이터:", json.datasets?.noticeSpecial);

// 2. 데이터 매핑 확인
const modelRows = Array.isArray(json.datasets?.noticeModel) 
  ? (json.datasets.noticeModel as NoticeModelApiRow[]) 
  : [];
console.log("모델 행 수:", modelRows.length);
console.log("첫 번째 모델:", modelRows[0]);

// 3. buildApplicationRows 결과 확인
const { rows, missingSpecialRequests, totals } = buildApplicationRows(
  modelRows, 
  competitionRows, 
  specialRows
);
console.log("생성된 행 수:", rows.length);
console.log("합계 행:", totals);
```

### 2. 경쟁률이 0으로 표시되는 경우

**증상:**
- 경쟁률이 "0.00 : 1"로 표시됨
- 접수 건수가 표시되지 않음

**원인:**
- 경쟁률 데이터가 올바르게 매핑되지 않음
- `compMap`에 데이터가 누적되지 않음

**해결 방법:**

```typescript
// 경쟁률 데이터 매핑 확인
competition.forEach((item) => {
  const modelNo = String(item.MODEL_NO ?? item.HOUSE_TY ?? "").trim();
  console.log("경쟁률 아이템:", {
    modelNo,
    stageCode: item.SUBSCRPT_RANK_CODE,
    region: item.RESIDE_SECD,
    target: item.SUPLY_HSHLDCO,
    request: item.REQ_CNT,
  });
  
  // ... 매핑 로직 ...
});

// compMap 확인
console.log("compMap:", Array.from(compMap.entries()));
```

### 3. 특별공급 데이터가 누락되는 경우

**증상:**
- 특별공급 접수 건수가 "-"로 표시됨
- 카테고리별 내역이 표시되지 않음

**원인:**
- `noticeSpecial` API 호출 실패
- 데이터 매핑 키 불일치 (`MODEL_NO` vs `HOUSE_TY`)

**해결 방법:**

```typescript
// 특별공급 데이터 확인
special.forEach((item) => {
  const keys = [
    String(item.MODEL_NO ?? "").trim(), 
    String(item.HOUSE_TY ?? "").trim()
  ].filter((key) => key.length > 0);
  console.log("특별공급 키:", keys);
  console.log("특별공급 데이터:", item);
});

// specialMap 확인
console.log("specialMap:", Array.from(specialMap.entries()));

// 매핑 로직 확인
const specialAgg = specialMap.get(modelNo) ?? specialMap.get(houseType);
console.log("특별공급 집계:", { modelNo, houseType, specialAgg });
```

### 4. 합계 행이 올바르게 계산되지 않는 경우

**증상:**
- 합계 행의 값이 0으로 표시됨
- 경쟁률이 계산되지 않음

**원인:**
- `reduce` 함수에서 초기화 문제
- 경쟁률 계산 로직 누락

**해결 방법:**

```typescript
// 합계 계산 과정 확인
const totals = rows.reduce<ApplicationRow | null>((acc, row, index) => {
  console.log(`합계 계산 ${index + 1}/${rows.length}:`, {
    acc: acc ? {
      supplyGeneral: acc.supplyGeneral,
      supplySpecial: acc.supplySpecial,
    } : null,
    row: {
      supplyGeneral: row.supplyGeneral,
      supplySpecial: row.supplySpecial,
    },
  });
  
  // ... 누적 로직 ...
  
  return acc;
}, null);

// 경쟁률 계산 확인
if (totals) {
  console.log("경쟁률 계산 전:", {
    rank1: totals.stages.rank1,
    rank2: totals.stages.rank2,
    total: totals.stages.total,
  });
  
  fillRate(totals.stages.rank1);
  fillRate(totals.stages.rank2);
  fillRate(totals.stages.total);
  
  console.log("경쟁률 계산 후:", {
    rank1: totals.stages.rank1,
    rank2: totals.stages.rank2,
    total: totals.stages.total,
  });
}
```

### 5. 면적 계산이 잘못되는 경우

**증상:**
- 전용평형이 올바르게 표시되지 않음
- 면적 단위 변환 오류

**원인:**
- 평 변환 비율 오류 (3.3058)
- `toFloat` 함수 오류

**해결 방법:**

```typescript
// 면적 계산 확인
const areaSqmRaw = toFloat(model.SUPLY_AR);
console.log("면적 원본:", model.SUPLY_AR);
console.log("면적 변환:", areaSqmRaw);

const areaSqm = areaSqmRaw > 0 ? areaSqmRaw : null;
const areaPyeong = areaSqm ? areaSqm / 3.3058 : null;
console.log("전용면적 (㎡):", areaSqm);
console.log("전용평형 (평):", areaPyeong);
```

---

## 참고 자료

### 관련 문서

- [청약홈_API_연동_완전가이드.md](./청약홈_API_연동_완전가이드.md) - 기본 API 연동 가이드
- [모집공고_목록_구현_완전가이드.md](./모집공고_목록_구현_완전가이드.md) - 목록 구현 가이드

### API 문서

- 공공데이터포털: https://www.data.go.kr
- ApplyhomeInfoDetailSvc 문서
- ApplyhomeInfoCmpetRtSvc 문서

---

## 버전 정보

- **문서 버전**: v1.0
- **Next.js**: 14.2.33
- **React**: 18.3.1
- **TypeScript**: 5.6.3
- **작성일**: 2025-01-15

---

## 작성자 노트

> 이 문서는 모집공고 상세 정보 구현에 필요한 모든 내용을 단계별로 정리했습니다.  
> 특히 `buildApplicationRows` 함수의 복잡한 데이터 통합 로직을 상세히 설명했습니다.  
> 각 섹션은 독립적으로 이해할 수 있으며, 전체 흐름을 파악한 후 구현하시기 바랍니다.  
> 트러블슈팅 섹션은 실제 개발 중 발생할 수 있는 문제들을 해결하는 데 도움이 될 것입니다.

